const express = require("express");
const router = express.Router();
const { f } = require("../controller/data.controller");   

// REST REQUEST FOR USERS.

// REST REQUEST FOR CAMPAIN.


module.exports = router;